Put this line

127.0.0.1 php.local

in your /etc/hosts

=====================

In .env file, change var "PROJECT_PATH" for your project path

=====================

startContainers accept one param, if param equal 1, run in background mode:

./startContainers.sh 1

=====================
a
